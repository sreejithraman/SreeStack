interface:
  display_name: "Review Fix Loop"
  short_description: "Review, fix, and verify a local diff"
  default_prompt: "Use $review-fix-loop to review, fix, and verify the current diff until it is stable or blocked."
