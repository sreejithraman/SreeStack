interface:
  display_name: "Swarm and Push"
  short_description: "Build and deliver merge-ready pull requests"
  default_prompt: "Use $swarm-and-push to build this task and deliver its pull requests to the requested stopping point."
