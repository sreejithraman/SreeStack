interface:
  display_name: "Execute Goal"
  short_description: "Run one goal and prove the result"
  default_prompt: "Use $execute-goal to write one measurable objective, do the work, check the result, and update the goal state."
